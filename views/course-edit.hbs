<h1>Edit course {{course.title}}</h1>

<form action="/courses/edit" method="POST">
    <div class="row">
        <div class="input-field">
            <input value={{course.title}} name="title" id="title" type="text" class="validate" required>
            <label for="title">Название курса</label>
            <span class="helper-text" data-error="Введите название"></span>
        </div>

        <div class="input-field">
            <input value={{course.price}} name="price" id="price" type="number" class="validate" required min="1">
            <label for="price">Цена</label>
            <span class="helper-text" data-error="Введите цену"></span>
        </div>

        <div class="input-field">
            <input value={{course.img}} name="img" id="img" type="text" class="validate" required>
            <label for="img">Image url</label>
            <span class="helper-text" data-error="Введите url"></span>
        </div>

        <input type="hidden" name="id" value={{course.id}}></input>
        <button class="btn btn-primary">Изменить</button>
</form>